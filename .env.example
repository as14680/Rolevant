ANTHROPIC_API_KEY=sk-ant-...

# Optional: JSearch on RapidAPI (free tier: 200 req/month) for LinkedIn/Indeed
# JSEARCH_API_KEY=your_rapidapi_key_here

# Deployment: point to a persistent volume so the DB survives restarts
# DATA_DIR=/data

# Set to "production" to disable hot-reload
# ENVIRONMENT=production
